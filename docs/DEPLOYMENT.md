# Deployment Guide
Use PHP 8.4 FPM, MySQL 8, queues, scheduler, Reverb, HTTPS, and S3-compatible storage. Run `php artisan config:cache route:cache view:cache` during release and execute migrations with backups.
