# Installation Guide
Copy `.env.example` to `.env`, run `composer install`, `npm install`, `php artisan key:generate`, `php artisan migrate --seed`, `npm run build`, then serve with `php artisan serve` or Docker Compose.
