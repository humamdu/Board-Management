# API Documentation
All endpoints are JSON APIs protected by Sanctum. Key routes: `GET /api/boards`, `POST /api/boards`, `GET /api/boards/{board}`, `POST /api/boards/{board}/cards`, `PATCH /api/cards/{card}`, `PATCH /api/cards/{card}/move`, `GET /api/search`, and `GET /api/dashboard`.
